"use server";

import { revalidatePath } from "next/cache";

// This file can be removed or kept for other non-auth-related server actions if needed.
// For now, it is empty as the logic was moved to client.tsx to call Cloud Functions directly.
