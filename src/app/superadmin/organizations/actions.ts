"use server";

// This file is intentionally blank.
// The Firebase SDK's httpsCallable function, which is needed to call Cloud Functions
// with the user's authentication context, only works on the client-side.
// Therefore, the logic for changing an organization's status has been moved
// to the client component (`client.tsx`).
